{
  "name": "requation",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve dist -l 3000"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.101.1",
    "csv-parse": "^6.2.1",
    "dotenv": "^17.4.0"
  }
}
